# Signal Service
# TODO: Implement in Session 2

"""
class SignalService:
    async def get_list(db, corp_id, category, severity, status) -> list[Signal]
    async def get_by_id(db, signal_id) -> Signal | None
    async def update_status(db, signal_id, status) -> Signal
    async def dismiss(db, signal_id, reason, user_id) -> Signal
    async def get_stats(db, corp_id) -> dict
"""
