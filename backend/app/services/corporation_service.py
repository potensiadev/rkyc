# Corporation Service
# TODO: Implement in Session 2

"""
class CorporationService:
    async def get_list(db, skip, limit, status, search) -> list[Corporation]
    async def get_by_id(db, corp_id) -> Corporation | None
    async def get_by_biz_no(db, biz_no) -> Corporation | None
    async def create(db, corp_in) -> Corporation
    async def update(db, corp_id, corp_in) -> Corporation
    async def delete(db, corp_id) -> bool  # soft delete
"""
